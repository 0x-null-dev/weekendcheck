services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: weekendcheck
      POSTGRES_PASSWORD: weekendcheck_dev
      POSTGRES_DB: weekendcheck
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
